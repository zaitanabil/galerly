/**
 * Environment Configuration
 * This file should be loaded before config.js
 * 
 * IMPORTANT: Set these values via GitHub Secrets or environment variables
 * DO NOT commit hardcoded secrets to the repository
 */

// API Base URL - must be set via environment variable or GitHub Secrets
window.API_BASE_URL = window.API_BASE_URL || '';

// Stripe publishable key - must be set via environment variable
window.STRIPE_PUBLISHABLE_KEY = window.STRIPE_PUBLISHABLE_KEY || '';

